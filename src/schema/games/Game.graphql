type Console {
    id: String!
    name: String!
}

type Game {
    id: String!
    name: String!
    console: Console
}

type Query {
    getGames: [Game]
    getGamesByConsoleId(consoleId: String!): [Game]
}
